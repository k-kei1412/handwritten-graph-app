# 開発ログ

「手書き数式からインタラクティブな2Dグラフを生成するWebアプリ」の開発記録。

- リポジトリ: https://github.com/k-kei1412/handwritten-graph-app
- 公開URL: https://k-kei1412.github.io/handwritten-graph-app/
- 対象環境: iPad Safari(タッチ/Apple Pencil操作前提)

---

## 概要・機能

- 左ペイン: 手書きキャンバス(ペン/消しゴム/矩形選択/投げ縄選択)
- 右ペイン: インタラクティブなグラフ(パン/ピンチズーム/直線のドラッグ編集)
- 選択した手書き領域の画像を Base64 化し、Gemini API (`gemini-2.5-flash`) に送信して
  `{"type":"line","a":..,"b":..,"c":..}`(一般形 `ax+by=c`)を認識
- 認識した直線をグラフに描画し、端点(ハンドル)をドラッグすると係数がリアルタイム更新
- Gemini APIキーは LocalStorage に保存(サーバー送信なし)

ファイル構成: `index.html` / `style.css` / `app.js` の3ファイルのみで完結する静的サイト。

---

## 開発の経緯

### 2026-07-24 初期実装 (`330bf0d`)
- 3ファイル構成で機能一式を実装。
- ローカル(`python -m http.server`)+ Chrome自動操作で動作確認:
  - ペン/消しゴム描画、矩形選択、画像抽出(Base64 PNG化)は問題なく動作。
  - グラフの直線描画・ハンドルドラッグでの係数リアルタイム更新・パン・ホイールズーム・
    APIキー保存を確認。
  - 実装中に見つけて即修正したバグ:
    - `selectTool('pen')` の初期呼び出しが `selection` 変数の宣言(TDZ)より前に
      実行されるエラー → 初期化順序を修正。
    - 直線の初期ハンドル配置が急な傾きの場合に画面外に出る → ハンドル配置をビューポート
      に対する直線クリッピング(`clipLineToWorldRect`)に変更。
    - 係数の符号表示("+ -7.42y" のような二重符号)→ 表示フォーマットを修正。

### GitHub公開時のトラブル: legacy Pages ビルドがハング (`59293cd`)
- `gh repo create` → push → Pages有効化までは完了したが、ビルドが `building` のまま
  進行しなくなった(`duration:0` のまま更新されない)。
- 原因調査:
  - push時・ビルド強制実行時に GitHub 側で `Internal Server Error` / `502` が発生。
  - `pages-build-deployment` ワークフローの `deploy` ジョブが完了せず、後続の
    デプロイが `Please cancel <sha> first` でブロックされていた。
  - → GitHub側インフラの一時的な不調と判断。
- 対応:
  1. スタックしていた legacy ビルドの実行を `gh run cancel` でキャンセルしてロック解除。
  2. Pages のビルド方式を legacy から GitHub Actions ベース
     (`actions/configure-pages` + `actions/upload-pages-artifact` +
     `actions/deploy-pages`)に切り替え、`.github/workflows/pages.yml` を追加。
  3. 手動トリガーでデプロイ成功を確認、以後は push のたびに自動デプロイされる構成に。

### 不具合報告1: グラフを動かすと直線が「途中で切れる」(`5d64cc4`)
- 症状: パン操作でハンドルから離れると、直線(本来は無限直線)がハンドル間の
  「線分」としてしか描かれておらず、途中で終わって見える。
- 原因: `drawGraph()` が `line.p1`〜`line.p2` の座標をそのまま結んで描画していた
  (直線の方程式ではなく、固定された2点間の線分として扱っていた)。
- 修正: 毎フレーム、現在の表示範囲(ビューポート)に対して直線の方程式 `a,b,c` を
  `clipLineToWorldRect()` で再クリッピングし、ハンドルの位置に関係なく画面端から端まで
  描画するように変更。ハンドル自体は引き続きドラッグ用の制御点として元の座標に保持。
- 検証: JS越しに極端なパン量(ハンドルが完全に画面外)を再現し、直線が画面端まで
  正しく延びること、係数表示もハンドルが見えない状態で正しく更新されることを確認。

### 不具合報告2: パンで下部が「バグる」+ドラッグを1マス単位に (`5f787ca`)
- 症状: グラフを大きくパンすると、画面下部の描画が崩れる。
- 原因調査:
  - x軸/y軸の描画・目盛りラベルが `screenY(0)` / `screenX(0)` の座標を無条件に使用しており、
    パンで軸が画面外はるか遠くに移動すると、その巨大な座標をそのまま `moveTo`/`lineTo`/
    `fillText` に渡していた。
  - 直線のハンドル(丸)も同様に、パン後の生の world 座標をそのまま `arc()` に渡していた。
  - Safari/WebKit は極端に大きな座標でのcanvas描画时に描画が破損することがあるため、
    これが崩れの原因と推測。
- 修正:
  - 軸線・軸ラベル・ハンドルそれぞれについて、画面から一定距離(±1000px)以上離れて
    いる場合は描画自体をスキップするガードを追加。
  - 併せてユーザー要望により、ハンドルドラッグ時に現在のグリッド間隔(`niceStep`)に
    座標を丸める「1マスずつ」のスナップ移動を実装。
- 検証: `view.panY += 50000` のような極端な状態で `drawGraph()` が例外なく完了し、
  かつ通常時の軸描画(ピクセル色サンプリングで確認)が変わらないことを確認。
  グリッドスナップは丸め計算を直接検証。

### 不具合報告3: パン中に画面下部でハンドルが何重にも残像化する (`5ed1397`)
- 症状: スクリーンショット添付により、パン中にハンドル(丸)が水平方向に何重にも
  重なった「残像」のような帯として描画されるバグを確認。
- 原因:
  - `pointermove` イベントはiPadのタッチサンプリング頻度が高く、画面のリフレッシュ
    レート(60Hz)より高頻度で発火することがある。
  - 従来は `pointermove` のたびに同期的に `drawGraph()`(クリア→再描画)を呼んでいたため、
    Safari/WebKit側で前フレームの描画がまだ完了していない状態で次のクリア/再描画が
    走り、フレーム合成に失敗して残像(ゴースト)が発生していたと推測。
- 修正: パン・ピンチズーム・ホイールズームの各イベントハンドラでは、状態(pan/scale)の
  更新のみ行い、実際の描画は `scheduleGraphRedraw()` で `requestAnimationFrame` に
  1回だけスケジュールするように変更(1フレームにつき最大1回の `drawGraph()` に集約)。
- 検証: 50回連続で pan 状態を更新しても `requestAnimationFrame` のスケジュールが
  ちょうど1回に収束することを確認。

### 不具合報告4: 数式追加でパネルが伸びるとグラフ下部が「バグる」+ 直線を傾き維持のまま平行移動できるように (`f72a98f`)
- 症状: 数式認識で直線を追加していくと右下の数式リスト(`formula-panel`)がコンテンツ量に応じて
  伸縮するが、グラフの直線を動かす(パンする)とグラフ下部の表示が乱れる。
- 原因調査:
  - グラフ用canvasの実寸(`canvas.width/height`・`canvas.style.width/height`)は
    `fitCanvasToWrap()` で計算されるが、これが呼ばれるのは `window` の `resize` /
    `orientationchange` イベント時のみだった。
  - 数式リストが伸びて `canvas-wrap` (グラフ描画領域)がflexレイアウト上で縮んでも、
    ウィンドウ自体のサイズは変わらないため上記イベントが発火せず、canvasの実寸は
    古い(縮む前の大きい)ままになる。
  - `drawGraph()` は毎回 `canvas-wrap` の最新の実寸を取得して描画するため、描画内容
    自体は新しいサイズに収まるが、canvas要素そのものは古い(縮む前の)サイズのままな
    ので、その差分だけ数式リストの上に重なって表示され、パン操作で再描画されるたびに
    ズレが目立って「下部がバグる」ように見えていた。
  - JSでの計測 (`canvas-wrap`の実測高さ vs `canvas.style.height`) で実際に値が乖離する
    ことを確認して原因を特定。
- 修正:
  - `ResizeObserver` で左右それぞれの `canvas-wrap` 自体のサイズ変化を直接監視し、
    (原因を問わず)実寸が変わるたびに `layoutAll()` を呼んでcanvasを再フィットする
    ように変更。`window` の `resize`/`orientationchange` リスナーは維持しつつ補完。
  - 併せて、直線の操作性向上のためユーザーから依頼のあった機能を追加: 従来は2つの
    端点ハンドルをドラッグして傾きを変えることしかできなかったが、直線の胴体
    (ハンドル以外の部分)をドラッグすると、p1・p2を同じ量だけ平行移動させることで
    傾き(係数a,bの比)を維持したまま位置(切片c)だけを動かせるようにした。
    ヒットテストは直線の方程式に対する点と直線の距離をワールド座標で計算し、
    `view.scale` を掛けてスクリーンpx換算することでズーム倍率に依らず一定の当たり
    判定幅になるようにした。
- 検証: Chrome自動操作でJS越しに数式(直線)を複数追加して数式リストを伸ばし、
  `canvas-wrap`実測高さと`canvas.style.height`が常に一致することを確認。パン操作後も
  境界がズレないこと、直線の胴体をドラッグするとa,bを維持したままcのみ変化すること、
  ハンドルドラッグでは従来通り傾きが変わることをそれぞれ座標ログで確認。

### 不具合報告5: 端点ハンドルによる傾き変更を廃止 (`5b7e65d`)
- ユーザー要望: 「傾きを変える2点(ハンドル)」自体を削除し、直線をどこでも触って
  平行移動するだけ(傾きは常に固定)というインタラクションに一本化してほしい。
- 修正: `hitTestHandle()` と、それに紐づく描画(ハンドルの丸)・ドラッグ分岐
  (`dragTarget.mode === 'handle'`)を全て削除。ドラッグ操作は常に「直線の胴体を掴んで
  平行移動」(`hitTestLineBody()`)のみになった。
- 検証: JS越しに直線を追加し、その胴体をドラッグしてa,bが変化しないままcのみ更新
  されることを確認。`hitTestHandle`が未定義になっている(完全に削除された)ことも確認。

### 機能追加: 複数式の同時認識 + 消しゴムのダブルタップメニュー(太さ/線分削除モード) (`af8943a`)
- 複数式の同時認識:
  - 従来はGeminiに「画像には数式が1つ含まれる」前提のプロンプトを送り、単一の
    `{"type":"line",...}` オブジェクトを期待していた。選択範囲に複数の式が
    書かれていても最初の1つしか認識できなかった。
  - プロンプトを「1つ以上の数式が含まれる」前提に変更し、JSON配列
    `[{"type":"line",...}, ...]` を返すよう指示。`callGemini()` は配列・単一
    オブジェクトのどちらの応答も受け付け(後方互換)、各要素を検証して配列で返す
    ようにした。`recognizeSelection()` は返ってきた分だけ `addLineFromCoeffs()`
    を呼び、複数の直線をまとめて追加する。
  - 検証: `fetch` をモックして配列レスポンス・単一オブジェクトレスポンスの
    両方で正しくパースされることを確認。
- 消しゴムのダブルタップメニュー:
  - 消しゴムボタンをダブルクリック/ダブルタップすると、消しゴムツールを選択した
    上で太さ調整スライダーと「ピクセル消しゴム」/「線分ごと削除」の切り替えを
    持つポップオーバーを表示するようにした。ポップオーバー外をタップすると閉じる。
  - 「線分ごと削除」モードでは、ストロークを1本1本の点群として保持している
    データ構造を利用し、タップ/ドラッグした位置に最も近い手書きストローク
    (点と線分の距離が閾値以内)を配列から削除して再描画する、いわゆる
    オブジェクト消しゴムとして機能する(消しゴム自身のストロークは対象外)。
  - 検証: JS越しに2本のストロークを描画し、線分削除モードで片方のストローク
    近くをタップすると該当ストロークだけが `strokes` 配列から消えることを確認。
    ピクセル消しゴムモードでは従来通り`destination-out`のストロークが追加され、
    スライダーで設定した太さが反映されることも確認。

### 機能追加: アプリアイコンの設定 (`7ead34f`)
- ユーザー提供の `fractal_icon_1.png`(1920×1920, 4.3MB)を元に、Pillowで
  32×32(`favicon-32.png`)/192×192(`icon-192.png`)/180×180
  (`apple-touch-icon.png`)にリサイズして書き出し。元の巨大ファイルをそのまま
  favicon等に使うと読み込みが重くなるため。
- `index.html` の `<head>` に `<link rel="icon">` (32px/192px) と
  `<link rel="apple-touch-icon">` (180px、iPadの「ホーム画面に追加」用)を追加。
- 検証: ローカルサーバー越しに各PNGが200で取得できること、`link[rel*="icon"]`
  が3つとも正しく解決されることをJS越しに確認。

### 機能追加: 消しゴムメニューを長押しに変更 / 手書き・グラフ欄のリサイズ / 数式パネルの副次表示を削除 (`9321b29`)
- 消しゴムメニューがiPadで開けない不具合:
  - `dblclick` はマウスのダブルクリックでは発火するが、iPad Safariでは
    ビューポート設定(`user-scalable=no`)でダブルタップズームを無効化している影響か、
    ボタンへのダブルタップから合成される `dblclick` が安定して発火しないケースが
    あった。
  - `pointerdown`→(500ms後)`pointerdown`起点からの移動が
    `LONG_PRESS_MOVE_TOLERANCE`(10px)を超えたらキャンセル、というタイマーベースの
    長押し検出に変更。`dblclick`リスナーはマウスユーザー向けに残した。
  - 検証: `setPointerCapture`合成イベント特有の`NotFoundError`を避けつつ
    pointerdown→タイマー予約→小さな移動では継続→大きな移動でキャンセル、を
    それぞれ確認。
- 手書き欄・グラフ欄のリサイズ:
  - `#workspace`内、両ペインの間に`#pane-resizer`(ドラッグハンドル)を追加。
    ドラッグで`#left-pane`に`flex: 0 0 <pct>%`を設定し(`#right-pane`は
    `flex: 1 1 auto`で残りを埋める)、横並び/縦積み(`@media max-width:820px`で
    自動切り替え)のどちらでも同じロジックで機能するようにした
    (`flex-basis`は常にflexコンテナの主軸に対して解決されるため)。
  - 各ペイン内のcanvasは既存の`ResizeObserver`(`canvasWrapResizeObserver`)が
    `canvas-wrap`のサイズ変化を検知して自動的に追従する。
  - 検証: `setPointerCapture`を一時的にスタブ化した上でpointerdown/move/upを
    合成イベントで発火させ、ドラッグ中に実際の幅がドラッグ位置の割合と一致すること、
    ドラッグ終了後もその幅が保持されることを確認。
- 数式パネルのグレーの副次表示(一般形 `ax+by=c`)を削除:
  - `formula-sub`要素とそれを生成していたコードを削除し、`lineLabel()`も
    傾き切片形式の文字列のみを返すよう簡略化。表示は主要な `y = ax + b` 形式のみに。

### 機能追加: 数式表示の簡略化(係数1・切片0の省略、分数表示) (`57c3ff8`)
- ユーザー要望:
  - x係数が1(または-1)のとき「1x」ではなく「x」/「-x」と表示してほしい。
  - 切片が0のとき「+ 0」を表示せず省略してほしい。
  - 分数として認識した傾き・切片は小数(例: 0.5)ではなく分数(例: 1/2)のまま
    表示してほしい。
- 実装:
  - 連分数展開によるベストな有理数近似 `toFraction(x, maxDen, eps)` と、
    それを使って整数ならそのまま・そうでなければ `p/q` 形式の文字列を返す
    `fmtFracAbs(n)` を追加。
  - `lineLabel()` を書き換え、傾きの絶対値が1のときは `x`/`-x` のみを出力、
    切片の絶対値が0のときは切片部分自体を出力しないように分岐を追加。
    傾き・切片の数値表示はすべて `fmtNum` から `fmtFracAbs` に置き換え。
    垂直線(`x = ...`)側も符号を保持したまま同じ関数で整形するよう修正
    (当初 `Math.abs` を通してから符号を戻すのを忘れ、負のx切片の符号が
    消えるバグを作ったが、実装直後の見直しで発見し修正)。
- 検証: Node上で `y=2x+1`・`y=x+3`(係数1の省略)・`y=2x`(切片0の省略)・
  `y=1/2x+3`・`y=1/3x-1/4`(分数表示)・`x=-3`・`x=1/2`(垂直線の符号と分数)・
  傾き0の水平線(従来通り`0x`のまま)を代表ケースとして計算し、期待通りの
  文字列になることを確認。

### 機能追加: リサイザーの当たり判定拡大 / 可動域拡張 / グラフ固定+マークアップ (`a07449e`)
- ユーザー要望: 手書き欄とグラフ欄の境界(`#pane-resizer`)の当たり判定が狭い、
  手書き欄をもっと小さくしてグラフ欄をもっと大きくできるようにしたい、
  グラフを固定した状態で書き込み(マークアップ)ができるようにしたい。
- リサイザー拡大: `.pane-resizer` の幅(横並び時)/高さ(縦積み時)を
  8px→20pxに拡大。ドラッグハンドルの視覚的な太さも4px→6pxに調整。
- 可動域拡張: `setLeftPanePercent()` の `clamp` 範囲を `(20, 80)` から
  `(10, 80)` に変更し、手書き欄をより小さく(グラフ欄をより大きく)
  リサイズできるようにした。
- グラフ固定+マークアップ:
  - グラフ用canvasの上に新しく `graph-ink-canvas`(ポインタイベントは
    `pointer-events: none` で親要素に委譲)を重ね、そこに書き込み用の
    ストロークを描画する構成に。既存のink-canvasの描画ロジック
    (`drawStrokePath`/`drawStrokeSegment`相当)を書き込み用に複製。
  - 「🔒 グラフ固定」ボタンで `graphLocked` を切り替え、ロック中は
    `rightWrap` のpointerdown/pointermove/pointerup/wheelの各ハンドラの
    先頭で分岐し、パン・ピンチズーム・ホイールズーム・直線ドラッグを
    すべて無効化。代わりにポインタ入力を書き込みストロークとして
    `annotationStrokes` に追加するようにした。
  - 「🖊️ 注釈消去」ボタンで書き込みを一括消去。
- 検証: Chrome自動操作で直線を1本追加→固定→ドラッグで書き込み→
  グラフの線自体は動いていないこと(`view.panX`/`panY`が不変)を確認。
  固定解除後は通常通りパンが機能すること(`view.panX`が変化すること)、
  リサイザーをドラッグすると手書き欄が20%未満(実測12.9%)まで縮むことも
  確認。

### 機能追加: グラフ固定時のペンFAB(色選択+消しゴム) (`598d49d`)
- ユーザー要望: グラフ固定時にグラフ右上へ丸いペンマークを表示し、押すと
  赤・青・緑・黄の4色から選べるように。そこに消しゴムボタンも追加してほしい。
- 実装:
  - `graph-canvas`を含む`canvas-wrap`内に、ロック中のみ表示される円形の
    FABボタン(`#btn-markup-pen`)を追加。現在の色(初期値は赤)を背景色として
    表示し、消しゴムモード中は🧹アイコン+グレー背景に切り替わる。
  - FABタップで開くポップオーバー(`#markup-popover`)に、既存の消しゴム
    ポップオーバーと同じ見た目のパターンで4色の丸いスウォッチボタンと
    「🧹 消しゴム」ボタンを配置。色を選ぶとペンモード+その色に、消しゴムを
    選ぶと`markupTool`を`'eraser'`に切り替えてポップオーバーを閉じる。
  - 消しゴムモードは、左ペインの「線分ごと削除」と同じ考え方で
    (点と線分の距離が閾値以内かを見る`distToSegment`/`dist`を再利用し)
    タップ/ドラッグした位置に最も近い`annotationStrokes`の1本を削除する
    `eraseAnnotationStrokeAt()`を実装。
- 実装中に見つけて修正したバグ:
  - ポップオーバーの位置決めで、`display:none`(`.hidden`)を外す前に
    `popover.offsetWidth`を読んでいたため常に0扱いになり、右寄せ位置が
    大きくずれていた → `hidden`クラスを外してから`getBoundingClientRect`/
    `offsetWidth`を読むよう順序を修正。
  - FABやポップオーバーが`canvas-wrap`(=`rightWrap`)の子要素だったため、
    それらへのクリックがバブリングして`rightWrap`の`pointerdown`
    ハンドラにも渡ってしまい、色選択やポップオーバー操作のたびに
    意図しない書き込みストロークが追加されていた → `rightWrap`の
    `pointerdown`ハンドラの先頭で、イベント発生元がFAB/ポップオーバー
    配下なら何もせず`return`するガードを追加。
  - 検証: Chrome自動操作でFABを開き、色スウォッチを直接`.click()`して
    `markupColor`が期待通り変わること、その後ドラッグで書いたストローク
    の`color`が選択した色になっていること、消しゴムモードに切り替えて
    ストローク付近をクリックすると`annotationStrokes`からそのストローク
    が消えること、上記2件のバグ修正後は色選択やFAB操作で
    `annotationStrokes`の件数が増えない(意図しない書き込みが発生しない)
    ことを確認。

---

## 既知の制約・今後の課題

- Gemini API呼び出し(実際の手書き認識精度)・iPad実機での基本動作はユーザー側で
  確認済み(2026-07-25時点)。
- ピンチズーム、および今回追加した長押しメニュー・ペインリサイズのドラッグ操作は、
  自動化ツールでは合成ポインタに `setPointerCapture` が使えず実際のタッチ操作を
  再現できないため、ロジック単体の検証(関数呼び出し・イベント配線の直接確認)に
  留まっている。いずれもiPad実機での最終確認が必要。
- 複数の直線を同時に扱えるが、UIとしての整理(色分け以上の区別)は未実装。
